<h1>My Activity</h1>

{{#if logs}}
<ul>
    {{#each logs}}
    <li>
        <a href="/activity/{{_id}}">{{movieTitle}} logged on {{date}}</a>
    </li>
    {{/each}}
</ul>
{{else}}
<p>You haven't logged any movies yet.</p>
{{/if}}