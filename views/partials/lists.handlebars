<h1>My Lists</h1>

{{#if lists}}
<ul>
    {{#each lists}}
    <li>
        <a href="/lists/{{this.id}}">{{this.title}}</a>
    </li>
    {{/each}}
</ul>
{{else}}
<p>You haven't created any lists yet.</p>
{{/if}}

<form method="GET" action="/lists/createList">
    <button type="submit">Create New List</button>
</form>